<div class="container">
<%= simple_form_for [@user, @garden] do |f| %>
 <%= f.input :title %>
 <%= f.input :address %>
 <%= f.button :submit, class: "btn btn-primary" %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
<% end %>
</div>

